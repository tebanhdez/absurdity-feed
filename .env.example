# Create a fresh key after revoking any key shared in chat.
OPENROUTER_API_KEY=replace_with_your_new_key

# Optional: defaults to OpenRouter's free-model router.
OPENROUTER_MODEL=openrouter/free

# Public address used in requests to OpenRouter.
PUBLIC_URL=http://at-server.local
